# when no input file is specified, process only these files:
@default_files = ( 'ConnectivityTestTechnicalNote.tex' );

$jobname = 'ConnectivityTest201812technicalNote' ;

# generate a PDF by default (1: generate it directly, with `pdflatex` or equivalent):
$pdf_mode = 1 ;

